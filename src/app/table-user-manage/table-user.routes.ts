import { Routes } from '@angular/router';
import { TableUserManageComponent } from './table-user-manage.component';

export const USER_MANAGE_ROUTES: Routes = [
  { path: '', component: TableUserManageComponent },
];

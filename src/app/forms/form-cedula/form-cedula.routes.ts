import { Routes } from '@angular/router';
import { FormCedulaComponent } from './form-cedula.component';

export const FORM_CEDULA_ROUTES: Routes = [
  { path: '', component: FormCedulaComponent },
];
